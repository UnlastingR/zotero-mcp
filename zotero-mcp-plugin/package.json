{
  "name": "zotero-mcp-plugin",
  "type": "module",
  "version": "1.5.1",
  "description": "Zotero MCP Plugin",
  "config": {
    "addonName": "Zotero MCP Plugin",
    "addonID": "zotero-mcp-plugin@autoagent.my",
    "addonRef": "zotero-mcp-plugin",
    "addonInstance": "ZoteroMCP",
    "prefsPrefix": "extensions.zotero.zotero-mcp-plugin"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/UnlastingR/zotero-mcp.git"
  },
  "author": "UnlastingR",
  "bugs": {
    "url": "https://github.com/UnlastingR/zotero-mcp/issues"
  },
  "homepage": "https://github.com/UnlastingR/zotero-mcp#readme",
  "license": "MIT",
  "scripts": {
    "start": "zotero-plugin serve",
    "build": "zotero-plugin build && tsc --noEmit",
    "lint:check": "prettier --check \"src/**/*.{ts,js}\" && eslint .",
    "lint:fix": "prettier --write \"src/**/*.{ts,js}\" && eslint . --fix",
    "release": "npm version patch --force -m \"Release v%s\" && git push --follow-tags",
    "release:beta": "npm version prepatch --preid beta --force -m \"Release v%s\" && git push --follow-tags",
    "prepare-release": "node scripts/prepare-release.js",
    "release:init": "node scripts/init-release.js",
    "test": "zotero-plugin test",
    "test:update": "node scripts/test-update.js",
    "update-deps": "npm update --save",
    "prepare": "cd .. && husky zotero-mcp-plugin/.husky"
  },
  "dependencies": {
    "zotero-plugin-toolkit": "^5.1.2"
  },
  "devDependencies": {
    "@types/chai": "^5.2.2",
    "@types/mocha": "^10.0.10",
    "@types/node": "^24.2.0",
    "@zotero-plugin/eslint-config": "^0.6.7",
    "chai": "^5.2.1",
    "eslint": "^9.32.0",
    "husky": "^9.1.7",
    "lint-staged": "^16.4.0",
    "mocha": "^11.7.1",
    "prettier": "^3.6.2",
    "typescript": "^5.9.2",
    "zotero-plugin-scaffold": "^0.8.6",
    "zotero-types": "^4.1.2"
  },
  "prettier": {
    "printWidth": 80,
    "tabWidth": 2,
    "endOfLine": "lf",
    "overrides": [
      {
        "files": [
          "*.xhtml"
        ],
        "options": {
          "htmlWhitespaceSensitivity": "css"
        }
      }
    ]
  },
  "lint-staged": {
    "src/**/*.{js,mjs,ts,tsx,json,md}": [
      "prettier --write"
    ]
  }
}
